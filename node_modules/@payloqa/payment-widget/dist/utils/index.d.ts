import type { PaymentData, PaymentRedirectStatus } from "../types/payment";
export declare const formatPhoneNumber: (phoneNumber: string) => string;
/**
 * Validates that a redirect URL is HTTPS
 */
export declare function validateRedirectUrl(url: string): boolean;
/**
 * Builds a redirect URL with query parameters based on payment status
 */
export declare function buildRedirectUrl(redirectUrl: string, paymentId: string, status: PaymentRedirectStatus, paymentData?: PaymentData): string;
//# sourceMappingURL=index.d.ts.map